define service {
    use                     generic-service
    host_name               {{HOSTNAME}}
    service_description     Network Connectivity
    check_command           check_network!{{TARGET_IP}}
}
