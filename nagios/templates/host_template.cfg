define host {
    use                     linux-server
    host_name               {{HOSTNAME}}
    alias                   Home Raspberry Pi
    address                 {{IP_ADDRESS}}
    max_check_attempts      5
    check_period            24x7
    notification_interval   30
    notification_period     24x7
}
